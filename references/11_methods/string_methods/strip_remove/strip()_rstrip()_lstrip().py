# To remove Whitespaces we use Strip() function
a = "               Hello, World!            "
print(a.strip()) # this removes left and right both whitespaces
print(a.rstrip()) # this only removes right whitespaces
print(a.lstrip()) # this only removes left whitespaces
